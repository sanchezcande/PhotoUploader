specVersion: "v1beta"
serviceId: "photo_uploader"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "photo_uploader-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
